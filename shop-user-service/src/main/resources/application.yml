server:
  servlet:
    context-path: /shop-user-service

spring:
  profiles:
    active: ${profileActive}

logging:
  config: classpath:logback.xml
  level:
    org:
      springframework:
        web: info

# sql xml文件位置
mybatis:
  mapper-locations: classpath*:mybatis/*.xml,classpath*:mybatis/*/*.xml